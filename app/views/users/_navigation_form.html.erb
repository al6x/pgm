<% if current_user %>
  <%= form_tag sessions_path, method: 'delete', class: "navbar-form navbar-right user-form", role: "form" do %>
    <button type="submit" class="btn btn-success">Sign out</button>
  <% end %>
  <p class="navbar-text navbar-right">Signed in as <%= current_user.name %></p>
<% else %>
  <!-- <%= form_tag sessions_path, class: "navbar-form", role: "form" do %>
    <div class="form-group">
      <%= text_field_tag :name, '', class: "form-control", placeholder: "Name" %>
    </div>
    <div class="form-group">
      <%= password_field_tag :password, '', class: "form-control", placeholder: "Password" %>
    </div>
    <button type="submit" class="btn btn-success">Sign in</button>
    <%= link_to 'Sign up', new_users_path, class: "btn btn-default", role: "button" %>
  <% end %> -->
  <div class="navbar-right">
    <%= link_to 'Sign in', new_sessions_path, class: "btn btn-default navbar-btn", role: "button" %>
    <%= link_to 'Sign up', new_users_path, class: "btn btn-default navbar-btn", role: "button" %>
  </div>
<% end %>